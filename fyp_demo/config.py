# Models user can select from the dropdown
# These must match the subfolder names inside data/videos/ and data/frames/
MODELS = [
    "early",
    "vit"
]

# Scenarios user can select — must match filenames (without extension)
SCENARIOS = [
    "scene1",
    "scene2",
]

# Root data folder
MEDIA_DIR = "data"